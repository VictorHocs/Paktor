// Generated by CoffeeScript 1.6.3
angular.module('PaktorApp', ['ui.router', 'ngAnimate', 'ui.bootstrap', 'ngScrollSpy', 'debug']).run(function($rootScope, $window) {
  return angular.element($window).on('resize scroll', function() {
    return $rootScope.$broadcast('animate');
  });
}).config(function($locationProvider) {}).controller('ModalVideoCtrl', function($uibModal) {
  this.open = function() {
    return $uibModal.open({
      templateUrl: 'myModalContent.html',
      controller: 'ModalVideoInstanceCtrl as videoInstance',
      size: 'lg'
    });
  };
}).controller('ModalVideoInstanceCtrl', function($modalInstance) {
  this.close = function() {
    return $modalInstance.dismiss('cancel');
  };
}).controller('AnchorScrollCtrl', function($anchorScroll, $location) {
  this.go = function(hash) {
    $location.hash(hash);
    return $anchorScroll();
  };
}).directive('clickToTop', function() {
  return function(scope, element) {
    return element.on('click', function() {
      return document.body.scrollTop = 0;
    });
  };
}).directive('animateEffect', function($timeout) {
  return function(scope, element) {
    var animate;
    animate = function() {
      var bottom, innerHeight, top, _ref;
      _ref = element[0].getBoundingClientRect(), top = _ref.top, bottom = _ref.bottom;
      innerHeight = window.innerHeight || document.documentElement.clientHeight;
      scope.isElementInViewport = top < innerHeight && bottom > 0;
      return scope.$apply();
    };
    $timeout(animate, 1000);
    return scope.$on('animate', animate);
  };
});
